{
  "name": "socket-input-params",
  "version": "1.0.1",
  "description": "A Node.js module used to receive input parameters via a Unix domain socket.",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "coverage": "nyc npm run test",
    "coverage-html-report": "npm run coverage && nyc report -r html",
    "prepublishOnly": "npm test"
  },
  "files": [
    "index.js"
  ],
  "keywords": [
    "Unix domain socket",
    "socket",
    "input",
    "params",
    "parameters"
  ],
  "author": "Cláudio de Castro",
  "engines": {
    "node": ">=8.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "command-line-args": "^5.1.1",
    "shell-parser": "^1.0.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.1.3",
    "nyc": "^15.1.0"
  },
  "repository": "git@github.com:blockchainofthings/socket-input-params.git"
}
